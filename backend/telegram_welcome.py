# Единственный текст приветствия по /start — только ▶️, без эмодзи нот.
# Используется в bot.py и server_lite.py.
WELCOME_MESSAGE = "▶️ TGPlay — музыкальный плеер\n\nНажми кнопку ниже, чтобы открыть:"

# Основной домен приложения. Управляется через WEBAPP_URL в backend/.env (на VPS).
# При смене домена — обновить WEBAPP_URL в /root/.tgplay/secrets.env и перезапустить.
import os as _os
WEBAPP_URL_CANONICAL: str = (
    (_os.getenv("WEBAPP_URL") or "").strip().rstrip("/")
    or "https://tgplay.fun"
)

# Текст кнопки меню бота (в чате и, где поддерживается, в списке чатов). До 32 символов.
MENU_BUTTON_TEXT = "PLAY"

# Имя бота в BotFather/поиске Telegram (до 64 символов). Выставляется через setMyName при старте.
BOT_NAME = "TGPlay — Музыка и плейлисты"

# About (короткий, до 120 символов) — выставляется в setMyShortDescription.
BOT_ABOUT_TEXT = "Музыкальный плеер без ограничений! Создай свой плейлист, слушай музыку в фоновом режиме и сохраняй любимые треки в кэш!"

# Description (полный текст в профиле бота) — выставляется в setMyDescription.
BOT_DESCRIPTION = """TGPlay — Твоя музыка без ограничений!

— Создай свой плейлист.
— Слушай в фоновом режиме.
— Сохраняй в кэш и слушай офлайн.
— Никакой рекламы и подписок.

Нажми Старт, чтобы запустить плеер!"""
